#!/bin/sh
msg=$(cat "$1")
if ! echo "$msg" | grep -qE '^(feat|fix|docs|refactor|test|ci|build|chore|perf|style|revert)(\([^)]+\))?: .{1,}$'; then
  echo "Commit message must follow Conventional Commits format"
  echo "Example: feat(doctor): add environment diagnostics"
  exit 1
fi
